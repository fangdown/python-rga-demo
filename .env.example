# Dev: use Vite proxy — see vite.config.ts (prefix /api → VITE_PROXY_TARGET)
VITE_RAG_BASE_URL=/api/v1/rag
VITE_APP_BASE=/

# Backend origin for proxy (Python RAG service)
VITE_PROXY_TARGET=http://127.0.0.1:8000

# Prod / direct: full URL from RAG_API.md
# VITE_RAG_BASE_URL=https://api.fangdu.chat/python/api/v1/rag
# VITE_APP_BASE=/python-rga/
